apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-cephfsplugin-provisioner
  namespace: rook-ceph
spec:
  template:
    spec:
      containers:
      - name: csi-addons
        args:
        - -controller-port=9070